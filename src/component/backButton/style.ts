import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  backBtn: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#fff",
    borderRadius: 18,
    padding: 6,
  },
  btnText: { fontSize: 16, marginLeft: 4 },
});

export default styles;
