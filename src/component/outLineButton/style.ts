import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  btn: {
    padding: 16,
    borderColor: "#ffa451",
    borderWidth: 1,
    borderRadius: 10,
    alignItems: "center",
    width: "100%",
  },
  btnText: {
    fontSize: 16,
    lineHeight: 24,
    color: "#FFA451",
  },
});
export default styles;
