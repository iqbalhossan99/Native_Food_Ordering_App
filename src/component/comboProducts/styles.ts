import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  combocontainer: {
    // paddingVertical: 40,
    paddingTop: 40,
  },
  comboTitle: {
    fontSize: 24,
    lineHeight: 32,
    // marginBottom: 24,
  },
  separator: {
    height: "100%",
    width: 16,
  },
});

export default styles;
